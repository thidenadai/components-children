import { View, Text } from "react-native";
import { styles } from "../styles/Styleshetts";
export default function TxtComponent() {
  return (
    <View style={styles.txtcomponent}>
      <Text>Faça login!</Text>
    </View>
  );
}
